# V3.8 정책금리 자체모델 격리 개선

## 안전 원칙
- Fed Funds 선물 시장 내재경로는 대표 전망으로 계속 유지합니다.
- 자체 예측모델은 별도 보조 신호이며, 시장경로를 자동으로 덮어쓰지 않습니다.
- `confidence.data_quality_*`와 `confidence.model_validation_*`를 분리했습니다.
- 과거의 `49/100`은 실제 예측정확도가 아니라 미검증 상태의 호환용 하드캡이었습니다. V3.8에서는 `model_validation_score`와 `data_quality_score`를 분리하고, 기존 `score`는 구형 소비자 보호용으로만 유지합니다.

## 자체모델 개선
- 직전 정책결정의 방향을 `policy_inertia`로 명시적으로 추가했습니다.
- FRED `DFEDTARU`/`DFEDTARL` 목표범위를 수집해 실제 FOMC 결정 라벨에 우선 사용합니다.
- 일별 정책/시장계열 보존기간을 늘려 60개 이상의 정례 FOMC 표본을 재구성할 수 있게 했습니다.
- Fed 페이지의 `Jan/Feb 31-1` 같은 월경계 회의 파싱을 지원합니다.
- 2015~2020 정례 FOMC 결정일을 감사된 일정으로 병합합니다.
- 발표시차를 반영한 재구성 OOS와 앞으로 누적되는 실시간 스냅숏 검증을 분리했습니다.
- 기존에 항상 false였던 `real_time_vintage`를 재구성 OOS 통과의 불가능한 필수조건으로 두지 않습니다.

## 검증 게이트
발표시차 OOS 통과 조건:
- 표본 >= 60
- 방향일치율 >= 60%
- Wilson 95% 하한 > 50%
- 다수결 기준 대비 방향 skill > 0
- Brier skill >= 10%

실시간 스냅숏 자체모델 검증은 별도 누적합니다.
